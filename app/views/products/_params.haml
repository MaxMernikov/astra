%table.table.params
  %tbody
    - Product::PARAMS.each do |param|
      - if (value = @product.send(param)).present?
        %tr
          %td= I18n.t("activerecord.attributes.product.#{param.to_s}")
          %td= value